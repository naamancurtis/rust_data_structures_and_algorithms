/// # Sliding Window Pattern
///
/// This uses a "Window" which slides along to look at sub-sections of a
/// collection without needing to calculate the whole sub-section each time
pub mod max_array_sum;
