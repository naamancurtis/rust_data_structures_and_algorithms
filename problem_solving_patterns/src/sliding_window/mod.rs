/// # Sliding Window Pattern
///
/// This uses a "Window" which slides along the collection to look at sub-sections
/// without needing to re-calculate the whole sub-section each time
pub mod max_array_sum;
pub mod min_sub_array_length;
